# 需求文档（历史归档）

> [!IMPORTANT]
> 本文档为项目早期（v0.1.0 前）的设计稿，**仅作历史参考**。
> 当前实际行为与最新功能以 [README.md](README.md) 与 [CHANGELOG.md](CHANGELOG.md) 为准。

## 已实现（现状速览）

- 启动器：环境体检 → 引导安装 Node.js（打开官方下载页）/ 一键安装 dsh（npm）→ 启动核心（零黑窗）
- 内嵌显示器：全窗口 WebView 显示官方 DSH 界面（127.0.0.1:3080）
- 核心管理：启动/停止（含外部启动的 dsh 只读身份校验，防误杀）
- 系统托盘：隐藏/恢复窗口、退出（停核心）
- 单实例：TCP 端口锁 + 唤醒
- 设置：✕ 关闭行为、双更新检查（dsh + 本软件）、镜像、壁纸
- 日志：全量运行日志（1MB 轮转）+ 崩溃钩子（PANIC 落日志）
- 卸载：npm 全局包 + 可选清理用户配置/npx 缓存

## 计划中（Roadmap）

- Node.js 安装自动化（当前为打开官方下载页引导）
- 健康守护（核心异常退出自动重启）
- 多实例工作区（--profile 切换）
- 生态快捷面板（会话/日志/配置目录直达）

## 不再实现的早期设想（删除说明）

- ~~npm.rs 模块~~：已并入 updater.rs / dsh_locator.rs
- ~~启动时自动拉起 dsh~~：改为用户点击"启动核心"（可控性优先）
- ~~500ms 轮询~~：前端实际 1 秒
- ~~托盘菜单含启动/停止~~：托盘仅"显示主界面"+"退出"
- ~~prevent_close（弹确认窗）~~：改为按设置直接退出（停核心）或隐藏托盘
