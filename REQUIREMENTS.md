# DeepSeek Harness Up — 需求规格说明书

> 独立 GUI 单 exe（非插件）：DSH 启动器 + 内嵌显示器 + 版本/卸载管理器
> 技术栈：**Tauri 2（Rust 后端）+ 系统 WebView2 前端**
> 状态：需求定稿（v1.1），已进入实现准备

---

## 1. 产品定位

一个**单独分发的 Windows exe**（~10MB），双击即用，承担三个角色：

| 角色 | 说明 |
|------|------|
| 启动器 | 拉起 dsh web；dsh 作为 exe 的**隐藏子进程**（CREATE_NO_WINDOW），任何窗口误关都不影响它 |
| 显示器 | 内嵌 WebView（WebView2）加载 http://127.0.0.1:3080/ ，与系统浏览器标签页隔离 |
| 管理器 | 版本查看/更新、运行状态、环境体检、安全卸载（可选清用户配置） |

**核心痛点解决：**
1. npx 慢启动 → 基于全局安装的 dsh 直接拉起（dsh-up 理念延续）
2. 浏览器标签混乱 → 内嵌 WebView 只显示 DSH 一个页面
3. cmd 窗口误关导致 dsh 被杀 → 无 cmd 窗口；exe 窗口关闭 = 最小化到托盘

---

## 2. 已确认的设计决策（定稿）

| # | 决策点 | 结论 |
|---|--------|------|
| D1 | 技术栈 | **Tauri 2**：Rust 后端 + 系统 WebView2（Win11 自带）；单 exe ~10MB、内存 ~60-80MB |
| D2 | 窗口关闭行为 | **最小化到托盘**，dsh 继续运行；托盘菜单才可真正退出（二次确认） |
| D3 | 更新方式 | **通知 + 手动按钮**：黄点提示新版本，用户点"更新"才执行 npm i -g |
| D4 | 卸载默认 | **默认只卸载全局包**，~/.dsh 用户配置默认保留；勾选才删 |
| D5 | Node 依赖 | **依赖系统 Node.js LTS**（用户自行安装/官网安装器），不做捆绑。exe 保持 ~10MB |
| D6 | 环境体检 | 首次运行/体检卡检测：Node / dsh 全局包 / WebView2 / 端口，缺项给一键处理按钮 |
| D7 | 单文件分发 | cargo tauri build 直接产出单 exe（资源嵌入），无安装器 |

---

## 3. 用户视角：六个真实使用场景

### 场景 1：早晨第一次用（核心路径，占 95% 使用）

```
用户：双击 dsh-up.exe

1. 窗口秒开（不等网络）—— 顶栏一屏可见三个信息：
   dsh 运行中  |  版本 0.1.1-rc.2  |  可更新 0.2.0（黄点）
2. 程序自动：dsh 没在跑 → 静默拉起 → 等 3080 就绪 → 内嵌页面加载（2~4 秒）
3. 用户直接开始用 DSH，与浏览器里使用无任何差别
```

### 场景 2：工作到一半关窗口（防误杀核心）

```
用户：点窗口 ✕（习惯动作）
程序：窗口收进托盘，任务栏保留图标；dsh 继续后台跑，对话进度不丢
用户：双击托盘图标 → 窗口原样弹出，内嵌页面回到当前对话
```

### 场景 3：DSH 挂了 / 端口被占（唯一会慌的场景，必须能自救）

```
情况 A：dsh 进程崩了 → 内嵌区域显示错误页：dsh 未运行（红点）+
          [重启 dsh] [查看日志] 按钮，不要求用户碰命令行
情况 B：3080 被占用（用户自己手动跑过 dsh）→ 提示：端口 3080 已被 PID xxxx 使用，
          [接管该实例] [停止它并重启] —— 让用户选择，绝不自动替用户杀进程
```

### 场景 4：新版本来了

```
某天打开 exe：版本卡片亮黄点 0.1.1-rc.2 → 可更新 0.2.0 [更新]
点[更新] → 日志区滚动 npm i -g … → 完成"已升级到 0.2.0，需重启 dsh 生效"
   [立即重启] [稍后]
无网络时：版本区静默显示"离线"，照常使用（黄点不打扰、不弹窗）
```

### 场景 5：卸载（最后一次使用）

```
打开卸载页 → 清单：移除全局 dsh（自动含命令行 shim）
   ☐ 同时清除用户配置 ~/.dsh（profiles、会话记录）【默认不勾】
   ☐ 同时清理 npx 残留缓存（246MB）【默认不勾】
   dsh 正在跑 → 提示"将先停止 dsh" → 确认
完成页："dsh 已卸载。本启动器 exe 可继续保留，随时一键重装 dsh"
```

要点：**exe 不是 dsh 的一部分**——卸载 dsh 不牵连 exe；想删 exe 直接删文件（portable 无安装痕迹）。

### 场景 6：全新机器（环境体检）

```
双击 exe → 体检卡：
   [x] Node.js  LTS — 缺失 → [下载并安装 Node.js]（打开官网安装器）
   [x] dsh 全局包   — 缺失 → [一键安装 dsh]（npm i -g，日志可见）
   [x] WebView2    — 缺失 → [安装 WebView2 Runtime]（官方 bootstrapper）
   [x] 端口 3080    — 空闲 → 无需处理
全部通过 → 直接进入正常流程 (场景1)
```

### 从场景沉淀的核心原则

| 原则 | 来源 | 界面体现 |
|------|------|---------|
| 双击即用 | 1、6 | 无配置、无向导，默认值开箱即用 |
| 状态永远可见 | 1、3 | 顶栏常驻：状态点 + 版本 + 更新提示 |
| 错误可控可救 | 3 | 任何失败显示原因 + 提供按钮，绝不裸奔 |
| ✕ 不等于关 | 2 | 托盘常驻；只有托盘"退出"才真正结束 |
| 默认保守 | 5 | 删除类操作默认不勾；停外部进程需确认 |
| 不打扰 | 4 | 更新用黄点不用弹窗；离线静默降级 |
| 一个窗口承载全部 | 全部 | 内嵌 DSH 占主体，控制靠顶栏+侧栏+托盘 |

### 界面构图（用户看到的）

```
+----------------------------------------------------------------+
| dsh-up   |  dsh 运行中  |  版本 0.1.1-rc.2  |  可更新 0.2.0 (黄点) |
+----------------------+-----------------------------------------+
| 控制台 启动/停止       |                                         |
| 更新                   |     内嵌 DSH 界面（主体，占满）         |
| 卸载                   |     http://127.0.0.1:3080/             |
| 体检                   |                                         |
+----------------------+-----------------------------------------+
  右上角：刷新 | 在浏览器打开 | 设置 | 托盘图标常驻
```

---

## 4. 功能需求

### 4.1 版本卡片（顶栏常驻）

- 本地版本：读 npm root -g/@deepseek-ai/dsh/package.json（文件读取，不启动进程）
- 远程版本：https://registry.npmjs.org/@deepseek-ai/dsh/latest，3s 超时，结果缓存 1 小时
- 有新版 → 黄点 + [更新] 按钮；更新执行 npm i -g @deepseek-ai/dsh@latest（日志实时滚动）
- dsh 运行中更新 → 完成后提示重启生效

### 4.2 进程管理（控制台）

- 状态检测三层：① 本工具 spawn 的 PID 存活 ② 127.0.0.1:3080 可连接 ③ netstat -ano 反查 3080 监听 PID
- 启动：隐藏窗口 spawn dsh web（CREATE_NO_WINDOW，见 5.1）
- 停止：自己启动的直接 kill；外部启动的需确认（场景3B）
- 未安装 dsh → 引导安装（场景6）

### 4.3 内嵌显示器

- 主视图 = WebView 加载 http://127.0.0.1:3080/（Tauri 2 窗口内多 webview 布局）
- 打开即同步：确保 dsh 运行（默认开，设置可关）→ 轮询 3080 就绪（500ms 间隔，上限 30s）→ 加载
- 错误页（场景3）+ 刷新按钮 + 在浏览器打开按钮

### 4.4 卸载管理（场景5流程）

- 影响清单：全局包 + PowerShell shim（npm 自动处理）+ 可选 ~/.dsh + 可选 npx 缓存
- 运行中先停 → npm uninstall -g → 按勾选删目录 → 完成页

### 4.5 环境体检（场景6）

- node --version / npm --version（系统环境）
- dsh 全局包存在性（npm root -g 目录检查）
- WebView2 注册表检测（HKLM/HKCU EdgeWebView Clients）
- 端口 3080 占用状态
- 每项：状态灯 + 缺失原因 + 一键处理按钮（打开官网/执行 npm i -g）

### 4.6 系统托盘

- 左键单击恢复窗口；右键菜单：显示主界面 / 打开 DSH UI（系统浏览器）/ 启动 dsh / 停止 dsh / 退出
- 退出需确认："退出 dsh-up？dsh 会继续后台运行"
- dsh 意外退出 → 托盘气泡通知

### 4.7 设置（v1.1，可后置）

- 端口（默认 3080）、启动时自动拉起 dsh 开关、更新检查开关

---

## 5. 关键技术设计（Tauri 2）

### 5.1 dsh 进程生命周期（核心）

```
dsh-up.exe（GUI 进程，无控制台）
  └─ std::process::Command::new(dsh).args(["web"])
        .creation_flags(CREATE_NO_WINDOW)  // Windows hideless
        → 隐藏子进程 [node dsh web]，无窗口、无 cmd，任何窗口误关无影响
主窗口 close 事件 → 隐藏 + prevent_close（D2 托盘化）
```

### 5.2 dsh 可执行解析

```
1. where.exe dsh → 找到 npm shim（全局安装）
2. npm root -g 读 package.json → 直接 node <root>/@deepseek-ai/dsh/lib/bin.js web
3. 均失败 → 未安装状态，进入体检引导
```

### 5.3 Rust 模块划分

```
src-tauri/src/
  main.rs          # 入口：窗口/tray/生命周期/单实例
  dsh_process.rs   # spawn(CREATE_NO_WINDOW)/kill/状态检测(三层)
  dsh_locator.rs   # 可执行解析 + 全局路径探测
  updater.rs       # 版本检查(ureq, 3s 超时) + npm 更新(cmd /C npm)
  uninstaller.rs   # npm uninstall + 可选清理(~/.dsh, npx 缓存)
  env_check.rs     # node/npm/dsh/webview2/port 五项体检
  npm.rs           # npm 调用统一封装（cmd /C，隐藏窗口，实时日志回传）
ui/                # 前端：纯 HTML+TS（无框架），编译进 exe 资源
```

### 5.4 已核实的路径事实

| 路径 | 说明 |
|------|------|
| ~/.dsh | 配置根（$DSH_HOME 覆盖） |
| ~/.dsh/profiles/ | 用户 profile（dsh-app-boot 实测） |
| %LocalAppData%\npm-cache\_npx | npx 246MB 缓存 |
| npmPrefix（AppData/Roaming/npm） | 全局包 + dsh.cmd/dsh.ps1 shim |
| 端口 3080 | dsh-cmdline：port ?? 3080 |

---

## 6. 非功能需求

| 项 | 要求 |
|----|------|
| 平台 | Windows 10/11 x64（WebView2 前置，缺失走体检引导） |
| 启动速度 | 冷启动 ≤ 3s：先渲染壳 UI，再后台校验版本/状态 |
| 依赖 | Node.js LTS（用户装）+ 官方推出的 dsh 全局包 |
| 数据安全 | 删除 ~/.dsh 前二次确认；卸载默认不删配置（D4） |
| 离线友好 | 所有网络操作失败静默降级 |
| 多实例 | 单实例锁：双击第二个 exe 只激活已有窗口 |

---

## 7. 交付物

1. 单 exe：src-tauri/target/release/dsh-up-desktop.exe（~10MB，双击即用）
2. 源码：Rust 后端 + 无框架前端（如上结构）
3. 与 CLI 版 dsh-up 的关系：独立；后续可抽公共逻辑

## 8. 非侵入边界（核心设计原则）

**定位**：外部拓展工具，非插件、非侵入 —— 目的是 dsh 大版本更新后工具不废。

| 允许（dsh 公共稳定边界） | 禁止（dsh 内部/数据） |
|---|---|
| CLI 命令（bin.js --profile web --no-open 等官方接口） | ~/.dsh 内部结构（profiles/patch/BOM等） |
| npm 生态（registry/安装/卸载——世界标准） | dsh 内部 API（/api 事件流等） |
| 只读观测（端口/netstat/进程命令行） | 修改 dsh 用户数据/配置/插件/皮肤 |
| 自有数据（settings/log/bg.png） | 自愈类写操作 |

**已落实**：is_dsh_pid（只读进程特征校验）；卸载/停止外部核心前必须校验是 dsh，否则拒杀。

## 9. 后续可扩展（v1.1+）

- [ ] 端口可配置
- [ ] 内置 portable Node（构建开关，分发场景）
- [ ] exe 自身自动更新
- [ ] 卸载前备份 ~/.dsh
- [ ] 代码签名（分发前）